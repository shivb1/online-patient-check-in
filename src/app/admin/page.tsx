import AdminClient from "./AdminClient";

export default function AdminPage() {
  return (
    <main className="min-h-screen flex justify-center p-6 bg-slate-50">
      <AdminClient />
    </main>
  );
}